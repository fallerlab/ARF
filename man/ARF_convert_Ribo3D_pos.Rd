% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ARF_platform.R
\name{ARF_convert_Ribo3D_pos}
\alias{ARF_convert_Ribo3D_pos}
\title{Convert 3D Ribosome distance file to target organism through rRNA alignments}
\usage{
ARF_convert_Ribo3D_pos(
  source_distance_file,
  source_rRNAs_fasta,
  target_species,
  target_rRNAs_fasta,
  rRNA_pairs = list(),
  source_positions = NULL,
  source_sets = NULL,
  type = "distances"
)
}
\arguments{
\item{source_distance_file}{Path to the \code{XXX.ARF.minimum_distances.csv} file generated by
\code{ARF_parse_PDB_ribosome()} for the source 3D ribosome.}

\item{source_rRNAs_fasta}{FASTA file containing the rRNA sequences of the source organism.}

\item{target_species}{ID string for the target species, e.g. \code{"mm"}, \code{"sc"}, \code{"dm"}.}

\item{target_rRNAs_fasta}{FASTA file for the rRNAs of the target organism. Should be the same file
used in rRNA fragment alignment.}

\item{rRNA_pairs}{List of 2-element character vectors matching source rRNA IDs to target rRNA IDs
(necessary for lift-over). Example:
\code{list(c("rRNA_28S", "dm_28S"), c("rRNA_18S", "dm_18S"))}. Pairs for rRNAs with identical
names in source and target are added automatically.}

\item{source_positions}{Named list of position vectors to convert (alternative input when
\code{type='positionset'}).}

\item{source_sets}{GSEA sets data.frame to convert, with \code{"ont"} and \code{"gene"} columns
(alternative input).}

\item{type}{Which output to return. Currently \code{'distances'} (returns wide
\code{RP_proximity_df}) or \code{'positionset'} (returns a named list of target position vectors).}
}
\value{
If \code{type='distances'}: a wide data.frame (\code{RP_proximity_df}) equivalent to that
from \code{ARF_parse_PDB_ribosome()}, with coordinates lifted over to the target organism. Also
writes two CSV files analogous to those from \code{ARF_parse_PDB_ribosome()}. If
\code{type='positionset'}: a named list of target rRNA position vectors (character, formatted as
\code{rRNA_resno}).
}
\description{
Aligns rRNAs from the source organism (used to generate the distance file) and a target
organism, then lifts over source-species 3D ribosome data to target-organism rRNA coordinates via
pairwise ClustalW alignment. Short gaps (< 5 positions) in the target coordinates are filled by
linear interpolation of adjacent distances.
}
\examples{
\dontrun{
ARF_convert_Ribo3D_pos("Ribosome.3D.4V6X.ARF.minimum_distances.csv", "4V6X.rRNAs.fasta", "dm",
  "drosophila_rRNAs.fa", rRNA_pairs=list(c("rRNA_28S","dm_rRNA_28S"),
  c("rRNA_18S","dm_rRNA_18S"), c("rRNA_5.8S","dm_rRNA_5.8S"), c("rRNA_5S","dm_rRNA_5S")))
}
}
\seealso{
\code{\link{ARF_parse_PDB_ribosome}}, \code{\link{dripARF_get_RP_proximity_sets}}
}
\keyword{3D}
\keyword{conversion}
\keyword{coordinates}
\keyword{liftover}
\keyword{rRNA}
