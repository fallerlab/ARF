% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ARF_platform.R
\name{ARF_parse_PDB_ribosome}
\alias{ARF_parse_PDB_ribosome}
\title{Parse 3D Ribosome data from PDB}
\usage{
ARF_parse_PDB_ribosome(
  species,
  PDBid,
  PDB_file = NULL,
  rRNAs_file = NULL,
  PDB_chains_2_RP_nomenclature = NULL,
  download_directory = getwd(),
  method = "dripARF_minDistances",
  out_prefix = "Ribosome.3D",
  duplication_resolve = min
)
}
\arguments{
\item{species}{ID for the species to be analyzed, e.g. \code{"human"} or \code{"hs"}.}

\item{PDBid}{ID for the 3D structure from RCSB, e.g. \code{"4V6X"} for human.}

\item{PDB_file}{Path to a locally supplied CIF file for the 3D ribosome (optional; downloaded
automatically if not provided).}

\item{rRNAs_file}{Path to a rRNA FASTA file previously generated by ARF for this structure
(optional; downloaded automatically from RCSB if not provided).}

\item{PDB_chains_2_RP_nomenclature}{Alternative data.frame mapping chain IDs to RP names and new
nomenclature. Required columns: \code{ID}, \code{RP_name}, \code{RP_new}, \code{chainID}.
If \code{NULL}, the built-in ARF nomenclature map is used for supported PDB IDs.}

\item{download_directory}{Directory for downloaded CIF and FASTA files (default: \code{getwd()}).}

\item{method}{Analysis method to use. Default: \code{"dripARF_minDistances"}, which calculates
minimum rRNA-to-RP Euclidean distances. No other options are currently implemented.}

\item{out_prefix}{Prefix for the output CSV filenames (default: \code{"Ribosome.3D"}).}

\item{duplication_resolve}{Function used to resolve duplicated chains (default: \code{min},
i.e. take the minimum distance across duplicate chains).}
}
\value{
A data.frame (\code{RP_proximity_df}) with rows = rRNA positions (row names formatted as
\code{rRNA_resno}) and columns = ribosomal protein names, containing minimum Euclidean distances
(Angstroms) from each rRNA N1 atom to each RP alpha-carbon. Two CSV files are also written:
\code{<out_prefix>.<PDBid>.ARF.minimum_distances.csv} (long format) and
\code{<out_prefix>.<PDBid>.ARF.minDist_matrix.csv} (wide matrix).
}
\description{
Downloads a CIF structure file from RCSB for the given PDB ID (or uses a supplied CIF
file), extracts rRNA N1 atom coordinates and ribosomal protein (RP) alpha-carbon coordinates, and
computes the minimum Euclidean distance (in Angstroms) from each rRNA position to each RP. Returns
a proximity matrix and writes two CSV output files.
}
\examples{
\dontrun{
ARF_parse_PDB_ribosome("hs", "4V6X")
}
}
\seealso{
\code{\link{ARF_convert_Ribo3D_pos}}, \code{\link{dripARF_get_RP_proximity_sets}}
}
\keyword{3D}
\keyword{PDB}
\keyword{distances}
\keyword{ribosome}
