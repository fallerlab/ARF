% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/driftARF.R
\name{driftARF}
\alias{driftARF}
\title{driftARF - detect ribosome heterogeneity that progresses with a continuous sample feature}
\usage{
driftARF(
  samplesFile,
  rRNAs_fasta,
  features,
  samples_df = NULL,
  organism = NULL,
  compare = NULL,
  QCplot = TRUE,
  targetDir = NA,
  exclude = NULL,
  measureID = "abs_cor_measure",
  ssRPSEAplots = FALSE,
  include_collision = FALSE,
  gsea_sets_RP = NULL,
  RP_proximity_df = NULL,
  gsea_sets_Collision = NULL
)
}
\arguments{
\item{samplesFile}{Path to the tab-separated samples file. Must contain the feature column(s)
named in \code{features}.}

\item{rRNAs_fasta}{FASTA file for the rRNAs of the organism (the same file used for alignment).}

\item{features}{Character vector of continuous, numeric column names in the samples file to
correlate against. One result block and CSV are produced per feature.}

\item{samples_df}{Optional pre-read samples data.frame (skips reading \code{samplesFile}).}

\item{organism}{Organism abbreviation: \code{"hs"}, \code{"mm"}, \code{"sc"}, or \code{NULL} when
supplying custom sets.}

\item{compare}{Optional column for the DESeq2 normalisation model. Default \code{NULL} uses an
intercept-only design (\code{~1}); no \code{group} column is required. Provide a column name
only to include that factor in the normalisation model.}

\item{QCplot}{Logical, whether to draw read-fragment QC plots (default \code{TRUE}).}

\item{targetDir}{Output directory (default: working directory).}

\item{exclude}{Character vector of sample names to exclude.}

\item{measureID}{Ranking statistic; default \code{"abs_cor_measure"}
(\code{abs(r) * -log10(p)}). Use \code{"cor_measure"} for the signed, directional statistic;
see \code{driftARF_build_measure()}.}

\item{ssRPSEAplots}{Logical. When \code{TRUE}, write a per-feature interaction plot of
\code{weighted.RPSEA.NES_randZ} vs \code{RPSEA.NES_randZ} (default \code{FALSE}).}

\item{include_collision}{Logical. When \code{TRUE}, ribosome collision sets are merged into the
RP sets (as in \code{dricARF}) so collision sets are scored alongside RP heterogeneity sets
(default \code{FALSE}).}

\item{gsea_sets_RP}{Custom RP position sets (\code{ont}/\code{gene}) including \code{Rand*}
controls; preset for \code{hs}/\code{mm}/\code{sc}.}

\item{RP_proximity_df}{Custom RP-rRNA proximity matrix; preset for \code{hs}/\code{mm}/\code{sc}.}

\item{gsea_sets_Collision}{Custom collision sets; preset for \code{hs}/\code{mm}/\code{sc}. Only
used when \code{include_collision = TRUE}.}
}
\value{
A data.frame of per-(set, feature) predictions; see
\code{\link{driftARF_predict_progression}}. Per-feature CSVs are written to \code{targetDir}.
}
\description{
Entry point for the driftARF subtool (\strong{D}ynamic \strong{RI}bosomal
\strong{F}eature \strong{T}racking). Given one or more continuous numeric features in the
samples file, driftARF correlates each per-position rRNA fragment abundance with the feature
and runs the ARF RPSEA + ORA engine to nominate position sets - ribosomal protein (RP)
heterogeneity sets, ribosome collision sets, or user-given sets - whose fragmentation tracks
the feature. It is the continuous-progression analogue of \code{\link{dripARF}} (RP
heterogeneity) and \code{\link{dricARF}} (collisions), which instead contrast discrete groups.
}
\examples{
\dontrun{
driftARF(samplesFile = "samples.tsv", rRNAs_fasta = "rRNAs/mouse_rRNAs.fa",
         features = c("age", "tumor_grade"), organism = "mm",
         targetDir = "driftARF_results/")
}
}
\seealso{
\code{\link{dripARF}}, \code{\link{dricARF}}, \code{\link{driftARF_predict_progression}}
}
\keyword{RPSEA}
\keyword{correlation}
\keyword{driftARF}
\keyword{feature}
\keyword{progression}
