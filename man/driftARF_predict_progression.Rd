% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/driftARF.R
\name{driftARF_predict_progression}
\alias{driftARF_predict_progression}
\title{Predict rRNA position sets whose fragment abundance tracks a continuous feature}
\usage{
driftARF_predict_progression(
  samples,
  rRNAs_fasta,
  features,
  rRNA_counts = NULL,
  dripARF_dds = NULL,
  compare = "group",
  organism = NULL,
  QCplot = FALSE,
  targetDir = NA,
  exclude = NULL,
  measureID = "abs_cor_measure",
  ssRPSEAplots = FALSE,
  gsea_sets_RP = NULL,
  RP_proximity_df = NULL,
  runID = "driftARF"
)
}
\arguments{
\item{samples}{Samples data.frame from \code{read_ARF_samples_file()}. Must contain the feature
column(s) named in \code{features}.}

\item{rRNAs_fasta}{FASTA file for the rRNAs of the organism.}

\item{features}{Character vector of column names in \code{samples} holding continuous numeric
features. driftARF loops over each feature and emits one result block (and CSV) per feature.}

\item{rRNA_counts}{Pre-computed rRNA count data.frame from \code{dripARF_read_rRNA_fragments()}
(optional).}

\item{dripARF_dds}{Pre-computed DESeq2 \code{DESeqDataSet} (optional). If \code{NULL} it is built
from the counts.}

\item{compare}{Column name used only to build the DESeq2 model for normalisation (default
\code{"group"}). The variance-stabilising transform is run with \code{blind=TRUE} so the
abundance scale used for correlation is not shaped by this grouping.}

\item{organism}{Organism abbreviation (\code{"hs"}, \code{"mm"}, \code{"sc"}) or \code{NULL} when
supplying custom sets.}

\item{QCplot}{Logical, passed to \code{dripARF_read_rRNA_fragments()} (default \code{FALSE}).}

\item{targetDir}{Output directory for per-feature CSVs (default: working directory).}

\item{exclude}{Character vector of sample names to exclude.}

\item{measureID}{Ranking statistic; see \code{driftARF_build_measure()} (default
\code{"abs_cor_measure"}).}

\item{ssRPSEAplots}{Logical. When \code{TRUE}, write a per-feature interaction plot of
\code{weighted.RPSEA.NES_randZ} vs \code{RPSEA.NES_randZ} (default \code{FALSE}).}

\item{gsea_sets_RP}{Position sets in \code{ont}/\code{gene} format including the \code{Rand*}
control sets (preset for \code{hs}, \code{mm}, \code{sc}; may already include merged collision
sets).}

\item{RP_proximity_df}{RP-rRNA proximity matrix (preset for \code{hs}, \code{mm}, \code{sc}).}

\item{runID}{Label used in output file names (default \code{"driftARF"}).}
}
\value{
A data.frame with one row per (set, feature), columns mirroring
\code{dripARF_predict_heterogenity()} (\code{comp} = feature name, \code{Description},
\code{ORA.*}, \code{RPSEA.NES}, \code{RPSEA.NES_randZ}, \code{RPSEA.padj}, ...), the ssRPSEA
corroboration columns \code{ssRPSEA.weight} and \code{weighted.RPSEA.NES_randZ}, plus
correlation context: \code{set.avg.r}, \code{set.avg.abs.r}, and \code{C1.avg.read.c}/\code{C2.avg.read.c}
(mean normalised abundance in the below-median vs at/above-median feature halves). The
\code{comp}/\code{C1}/\code{C2} columns make the result directly usable with
\code{dripARF_simplify_results()}, \code{dripARF_result_scatterplot()} and
\code{dripARF_result_heatmap()}.
}
\description{
Core driftARF routine. For each requested feature, correlates per-position
variance-stabilised rRNA fragment abundances with the (continuous, numeric) sample feature,
then runs the ARF RPSEA engine (\code{clusterProfiler::GSEA} over \code{gsea_sets_RP} with
z-scoring against the 99 \code{Rand*} control sets) plus an over-representation analysis (ORA)
over significantly-correlated positions. This is the correlation analogue of
\code{\link{dripARF_predict_heterogenity}}: only the per-position ranking statistic changes,
so the same RP heterogeneity, collision and user-given sets are supported.
}
\seealso{
\code{\link{driftARF}}, \code{\link{dripARF_predict_heterogenity}}
}
\keyword{RPSEA}
\keyword{correlation}
\keyword{driftARF}
\keyword{progression}
